version: '3.8'
services:
  app:
    build: .
    ports: ['3005:3000']
    environment: [NODE_ENV=production, MINIO_ENDPOINT=minio:9000]
    depends_on: [minio]
  minio:
    image: minio/minio
    ports: ['10005:9000', '10006:9001']
    command: server /data --console-address ':9001'

